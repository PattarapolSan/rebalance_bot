# News is now fetched by Claude via its built-in web search tool.
# This module is kept as a no-op stub for compatibility.

async def get_headlines(ticker: str, company_name: str = "", days_back: int = 1) -> list[str]:
    return []
